import styled from '@emotion/styled';
import Image from 'next/image';

const NextImageBox = styled(Image)`
    height: auto !important;
    position: relative !important;
`;

export default NextImageBox;
